---

import TerminalPrefix from './TerminalPrefix.astro';
import BlinkingCursor from './BlinkingCursor.astro';

const { isLastLine = false } = Astro.props;

--- 

<p>
  <TerminalPrefix /> 
  <slot />
  {isLastLine && <BlinkingCursor />}
</p>
